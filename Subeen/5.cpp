#include<stdio.h>
int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        while(tt--)
        {
            scanf("%c",&c);
        }
        printf("%d",result);
    }
    return 0;
}
